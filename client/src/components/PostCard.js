import React from 'react'
import {Card, Icon, Label} from "semantic-ui-react"

function PostCard({post:{body, craetedAt, id, username, likeCount, commentCount, like}}) {
    
    return (
        <div>
            
        </div>
    )
}

export default PostCard
